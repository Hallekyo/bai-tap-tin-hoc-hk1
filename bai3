program b3;
var x,y,z:real;
begin
  writeln('nhap gia tri x, y');readln(x,y);
  if sqr(x) + sqr(y) <= 1 
  then z:= sqr(x) + sqr(y)
  else  
  if y >= x
  then z:=x+y
  else z:=0.5;
  writeln('Vay gia tri cua z la: z=',z:5:2);
end.
